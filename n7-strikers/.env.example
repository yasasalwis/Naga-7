# General
ENVIRONMENT=development
LOG_LEVEL=INFO

# NATS
NATS_URL=nats://localhost:4222
NATS_CLUSTER_ID=n7-cluster

# Agent Identity
AGENT_TYPE=striker
AGENT_SUBTYPE=endpoint
AGENT_ID=striker-1
ZONE=default

# Core API
CORE_API_URL=http://localhost:8000/api

# Authentication
API_KEY_FILE=.agent_api_key

# Capabilities (comma separated if needed, but list handling in pydantic can be tricky depending on version, usually JSON string or repeated args work)
# For simplicity with pydantic v2 settings, JSON string is safe:
CAPABILITIES=["kill_process", "block_ip", "isolate_host", "unisolate_host"]
